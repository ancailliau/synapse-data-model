{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "../forms/inet.banner.json",
  "title": "banner",
  "description": "A JARM hash sample taken from a server.",
  "properties": {
    "server": {
      "description": "The server which presented the banner string.",
      "$ref": "../forms/inet.server.json"
    },
    "server:ipv4": {
      "description": "The IPv4 address of the server.",
      "type": "string"
    },
    "server:ipv6": {
      "description": "The IPv6 address of the server.",
      "type": "string"
    },
    "server:port": {
      "description": "The network port.",
      "$ref": "../forms/inet.port.json"
    },
    "text": {
      "description": "The banner text.",
      "$ref": "../forms/it.dev.str.json"
    }
  },
  "type": "object"
}